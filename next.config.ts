import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['v5.airtableusercontent.com', 'lh3.googleusercontent.com'],
  },
};

export default nextConfig;
